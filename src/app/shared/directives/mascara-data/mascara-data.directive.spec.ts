import { MascaraDataDirective } from './mascara-data.directive';

describe('CampoValidadeDirective', () => {
  it('should create an instance', () => {
    // const directive = new MascaraDataDirective();
    // expect(directive).toBeTruthy();
  });
});
