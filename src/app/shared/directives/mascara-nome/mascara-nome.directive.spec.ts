import { MascaraNomeDirective } from './mascara-nome.directive';

describe('MascaraNomeDirective', () => {
  it('should create an instance', () => {
    const directive = new MascaraNomeDirective();
    expect(directive).toBeTruthy();
  });
});
